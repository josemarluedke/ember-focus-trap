{{#docs-demo as |demo|}}
  {{#demo.example name="demo-example3.hbs"}}
    <p>
      <button onclick={{action this.activate}} class="button">
        Activate trap
      </button>
    </p>

    <div
      class="trap {{if this.isActive "is-active"}}"
      {{focus-trap
        isActive=this.isActive
        focusTrapOptions=(hash
          onDeactivate=(action this.deactivate)
          initialFocus="#initial-focusee"
        )
      }}
    >
      <p>
        Here is a focus trap
        <a href="#">with</a>
        <a href="#">some</a>
        <a href="#">focusable</a>
        parts.

        <br>
        Initially focused input
        <input type="text" id="initial-focusee">
      </p>
      <p>
        <button onclick={{action this.deactivate}} class="button">
          Deactivate trap
        </button>
      </p>
    </div>
  {{/demo.example}}

  {{demo.snippet "demo-example3.hbs" label="template.hbs"}}
  {{demo.snippet "demo-example3.js" label="component.js"}}
{{/docs-demo}}
